# Security documentation

Issue #4 owns the formal threat model, trust-zone data flows, sandbox policy,
signer protocol, key lifecycle, redaction standard, and security test backlog.
`SECURITY.md` contains the reporting policy and initial invariants until that work
lands.
